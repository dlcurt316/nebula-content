//SUBMIT A LINK TO THE GITHUB REPOSITORY CONTAINING THIS ASSIGNMENT
// Create a .js file that satisfies assignment below 
// Copy the final assignment text into this assignment to submit
// For each exercise you'll want to log the result to the console to check your work


//1. Create a set of variables that define an animal (age, height, name, etc...). Must be at least 6 variables.


//2. Create a set of variables that define a vehicle. Must be at least 6 variables.


const isMammal = true
console.log(isMammal)
const species = "whale"
const genus = "blue"
console.log(genus + species)
const gender = "male"
const color = "periwinkle"
console.log(color + gender)
const lengthNum = 49
const measurement = "feet"
console.log(lengthNum + measurement)
const age = 55
const ageMeasurement = "years"
console.log(age + ageMeasurement)

const year = 2023
const maker = "Kia"
console.log(year + maker)
const hue = "slver"
const brand = "niro"
console.log(color + brand)
const engine = "electric"
const type = "hybrid"
console.log(engine + type)